spring:
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  application:
    name: post-service
  profiles:
    active: ${ACTIVE_PROFILE}
  config:
    import: optional:configserver:http://localhost:${CONFIG_PORT}